---

- name: Configure and securise any Rpi

  hosts: all
  roles:
    - common

  become: yes

  vars_files:
    - variables.yml

- name: Setup and manage a download server

  hosts: download_servers
  roles:
    - download_server

  become: yes

  vars_files:
    - variables.yml
